# Memory

<!-- TODO: Long-lived facts and preferences.

     This file is loaded into context every conversation. Curate it. Watch the character cap
     (default ~20K, configurable as `bootstrapMaxChars` in ~/.openclaw/openclaw.json).
     Past the cap, the gateway silently truncates — older memories disappear.

     Sections:
       - User / operator facts (who, what they care about, what they don't)
       - Recurring patterns (this happens every Monday, that always fails on holidays)
       - Learned preferences (corrections the user has given that should stick)

     NEVER put secrets here. This file loads every conversation.
-->

## Operator facts

- [TODO]

## Recurring patterns

- [TODO]

## Learned preferences

- [TODO]
