# Tools

<!-- TODO: LLM-facing tool reference. One section per MCP tool the agent can call.
     For each tool: name, when to use, when NOT to use, example invocation.

     Tool registration lives in workspace/config/mcporter.json. This file describes
     the tools to the LLM in narrative form so it knows when to reach for which one.

     Audit: every tool listed here MUST have a corresponding entry in mcporter.json
     under the `mcpServers` key. Drift here surfaces as "agent doesn't know it has X".
-->

## Tool: [TODO tool_id_snake_case]

**When to use:** [TODO]

**When NOT to use:** [TODO]

**Example invocation:** [TODO — input shape]

## Tool: [TODO tool_id_snake_case_2]

[TODO]
