# Changelog

> Maintained automatically by `/ship`. Newest entries at the top.
> Each entry represents one shipped commit or completed phase.
>
> This is the primary "what has been built and when" reference for Claude sessions.
> Read alongside `docs/KB_8_Current_State.md` for project history without reading git log.
>
> To generate this file from existing git history, run `/changelog`.

---

<!-- /ship prepends new entries below this line -->

- 2026-07-07 — **v0.6.3 — skill-audit cleanup** (doc-only prose cleanup of all 25 command files + `docs/MULTI_AGENT_WORKFLOW.md`, net −370 lines; graded by an 8-judge panel — 6 Opus-family judges + 2 adversarial Fable adjudicators — over ~30 findings plus 5 new adjudicated to final verdicts). Zero guardrail cuts: every LESSONS-cited rule preserved (refutation blocks, seed prompts, fresh-context copies, destructive-op rules all kept) [verified: orchestrator reviewed the full diff in 3 passes against the verdict register]. Closed 3 drift vectors — the `/kickoff` embedded CLAUDE.md template (its DO-NOT copy had already drifted 7-vs-8 traps; now fills the on-disk `[TODO]`s in place rather than regenerating the DO-NOT / Custom Commands / Reference Documents blocks), hardcoded 22-command rosters → a `.framework-manifest.json` pointer, and a duplicated `KB_1` template [verified: diff]. Framework-wide app-blueprint web-residue sweep (audit-code globs, plan-review agent briefs, unify/update-kb/research vocabulary + dead `KB_7`/`gen-component` pointers) [verified: post-edit grep sweep — accepted residue only in `/visualize` + `/gen-test` teaching examples]. No runtime behavior changed. **Unverified at ship: SA-1 (awaits next downstream /kickoff)** — the fill-don't-regenerate rewrite is installed, not yet proven in a live run.
- 2026-07-07 — **v0.6.2 — wargame-planning intake** (spec `docs/wargame-planning-intake-spec.md`, LOCKED 2026-07-07 after a 3-agent plan-review; implementation diff reconciled hunk-by-hunk against the spec, then hardened by an 8-judge skill-audit panel with 4 tightenings applied). Extracts the transferable planning method from the "Fable Wargame Kit" (plan-vs-execute split, per-move expected-observations, pre-simulated failure branches, abort conditions, fail-loud requirements gate, blind-executability as a dispatch bar, model-aware executor briefs). All no new command (D1 closed as Option 1). Changes: **`/plan` + `MULTI_AGENT_WORKFLOW.md`** — per-step expected-observation/most-likely-failure lines and an **Abort conditions** section (blocked-escalate vs friction-push-through, each with a one-line classification test), both gated at Complexity ≥ Medium and kept judgment-based per `[SKILL-1]`/`[PROCESS-2]`. **`/plan-review` Step 6a** — a runtime-fork-trigger artifact class (an execution-time "if you observe X, take route B" line LOCKs IFF the trigger is observable, both routes are fully pre-designed, and the observable is runtime-evaluable — defer *which* route, never the *design* of a route). **`/implement`** — a per-step observation-confirmation consuming loop (confirm each step's expected observation against a tool result before proceeding). **`/orchestrate` + MAW** — blind-executability as a dispatch gate. **`OC_KB_05`** — new tier-role executor-brief section (planner-tier authoring guards / executor-tier briefing guards as durable principles, with named-model specifics in a dated, decays-with-releases Exhibit table). **`OC_KB_11`** — a BLOCKED-never-invent corollary (a missing required input is marked BLOCKED with the exact input logged, never fabricated). Sources: the wargame kit + 3 Anthropic system cards (Fable 5/Mythos 5, Opus 4.8, Sonnet 5) + the official Anthropic prompting docs + an independent red-team study; **15/15 load-bearing cites verified** [verified: adversarial page-level spot-check 2026-07-07]. Per `[PROCESS-1]` every change ships `Installed, not yet proven in a live run`: **Unverified at ship: WG-1..WG-7 (await first downstream firing)** — the wargame machinery is installed, not validated. No code beyond command/KB templates.
- 2026-07-07 — **v0.6.1 — T3 goal-mode video intake** (spec `docs/t3-video-2026-07-06-intake-spec.md`, LOCKED 2026-07-07; harvested from a practitioner goal-mode video via a 4-agent Opus grounding panel, with 3 Explore agents anchor-verifying every edit target at `file:line` before lock; rejected imports — per-model numeric score tables, "never use Haiku", clean-verification-as-under-pushing, unanimity-as-confidence, wall-clock effort thresholds, standalone prompt adapters — recorded in the spec's decisions table with reasons). One new command, rest prose: **`OC_KB_11`** — new §Autonomy budget (grant standing multi-step autonomy only when three structural properties hold: revertible non-prod deploy target, automated approval gate before each irreversible step, enumerated up-front permission grant; absent all three, stay turn-by-turn — containment earns the runway, not the model's judgment) + committed-ledger supervision surface (a bounded run writes its program as an in-repo checklist and commits completions, so a human watches progress land out-of-band; scoped to contained runs only). **`MULTI_AGENT_WORKFLOW.md`** — live-orchestration-vs-deterministic-workflow rule (a scripted fan-out fits exactly one shape, fan-out-and-verify with the `[PROCESS-3]` guard; checkpoint-driven programs run from the live PM session, workflows only *inside* them), Phase-8 **risk-targeted verification** (diff deployed..HEAD as the risk map; re-exercise *old* behaviors adjacent to the change; for autonomous multi-worker phases, verification spend exceeding implementation spend is expected — a clean result is honestly-clean per `falsification-primitive-spec.md` §2) + conditional worktree reconciliation, Phase-6 **split-verdict escalation** (disagreeing independent verifiers = positive escalation trigger, both positions quoted at the PM/human layer, never averaged away; mirrored in `/audit-code`'s Refutation Pass with the correlated-judges/F1 rationale) + per-model worker labeling. **`OC_KB_02`** — **delegation-skill archetype** (wrapping a peer agent CLI for capability gaps or token offload: boundary reconciliation vs. the MCP-preference note, exact-commands-as-highest-value content per `[SKILL-1]`, timeout recovery, field-failure-accretion lifecycle, disposable when the base model closes the gap) + **empty-result contract** (a delegating/reviewing skill must state "no findings" and name the inspected target — a silent empty return reads as an incomplete run; added to the validation checklist, `/gen-skill` Report template, and a `/gen-skill` description-breadth routing note). **`OC_KB_05`** — new §Routing philosophy (cost gates exploration, not what ships; configured routing is a default not a ceiling — standing permission to redo on a stronger model; task-shape tiers; anti-pattern: named-model exclusion rules rot each release). **`docs/LESSONS.md`** — new **`[PROCESS-7]`** (effort disproportion is an architecture signal — a fix costing wildly more than its request-class implies escalates to design scrutiny instead of a low-ceremony merge; measured by diff size / files touched / subsystems crossed, never wall-clock; disposable-probe instrument fenced to dev/staging) + `[PROCESS-1]` Corollary-1 trigger sharpen (a suspiciously-fast fix asserting an unexpected entity is a world-model claim — point at where the entity lives and read it first), both cross-referenced from `/debug`'s excuse table and `/audit-code`. **New `/triage` command** — stale-backlog triage (PRs/branches/work items) into action buckets with judge-verified verdicts and a mandatory `[PROCESS-3]` fail-loud coverage tally (N in → N verdicts out, dropped items surface as `UNVERIFIED` stubs); `/plan` gains a provenance-of-superseded-work check for plans it spawns. **`/kickoff` + CLAUDE.md** — Preferences **glossary** (define evaluative terms — "done", "clean", "taste", "good enough" — so task descriptions and routing rules interpret consistently). Per `[PROCESS-5]` this is a cross-context port (his: PR-workflow against auto-deploying staging; ours: prod-mutating OpenClaw agents) and per `[PROCESS-1]` **every change ships `Installed, not yet proven in a live run`** — the five behavioral claims are gated on smokes `T3V-1..T3V-5` in `docs/smoke-tests-pending.md`. No code beyond the command template.
- 2026-07-06 — **v0.6.0 — kai Jul 2–6 harvest intake** (spec `docs/kai-harvest-2026-07-06-intake-spec.md`, LOCKED 2026-07-06; derived from a 15-transcript kai-openclaw harvest 2026-07-02→06, stress-tested by a 12-judge Fable+Opus adversarial panel that corrected 5 synthesis-layer errors before the spec was assembled). All 20 edit blocks implemented across 9 files and review-verified `[verified: maintainer diff review — verbatim-once application, zero drift]`. Net of the release, all no-code: **`OC_KB_11`** — Primitive 8 gains the blocked-side sharpen (a "blocked"/"skipped" verdict is a capability claim held to the verify-by-attempt bar; an MCP-auth banner rules out one path, never the capability) + a seeded-negative anti-pattern bullet; Primitive 9's test-sentence gains its negative dual ("nor a blockage no attempt verified") and a **field closure** (the 2026-06-26 audit-row incident it was authored from is fixed and prod-verified downstream, 2026-07-02); Primitive 3 read-back scope now explicitly includes side-effect rows (D4, adopted at lock); new semantic-defaults anti-pattern (`DEFAULT`/`COALESCE` business-rule values on attribution/financial columns — the ~$97,156 two-headed trap, migration `20260625000001`). **`OC_KB_12`** — audit/status timestamps come from `now()`/column `DEFAULT`, never typed literals (R5); reconciliation hierarchy upgraded to **field-proven** (R3, Nash rate-churn 2026-07-02 — the documented mechanism firing, no new rule). **`CLAUDE.md`** — new DO-NOT trap (memory/bootstrap files hold rules + query paths, never system-of-record fact copies; R4-a, cross-ref bullet in `OC_KB_04` as R4-b) + the C4 scope-graduation conduct rule in Patterns (design sign-off ≠ deploy authorization; ask one explicit line before the first prod-mutating action) + LOCKED-≠-deploy clarification. **`docs/LESSONS.md`** — `[PROCESS-1]` **Corollary 3** (a negative status is a claim too; a false "blocked" cascades — three kai sessions each falsely declared Gmail blocked while their own greps surfaced the working CLI), `[PROCESS-4]` point (3) sharpen (Workflow `args` may arrive as a JSON string — guard-parse then fail loud, never `|| FALLBACK`; GitHub #67488), `[PROCESS-3]` corroboration append (two clean 8/8 small-flat-schema fan-outs — failure is intermittent, lesson stands, no demotion). **Command templates** — `/brainstorm` + `/investigate` deliverable footers (answering design decisions ≠ scope graduation), `/plan-review` Step 6 + `MULTI_AGENT_WORKFLOW.md` lockdown clarification (LOCKED certifies dispatch-readiness, not deploy authorization). Slice 6 (downstream kai fix prompt, Appendix E) is a handoff outside this repo. Deliberately deferred: R2 subtraction-inference (kai-only per `[PROCESS-5]`). **Per spec §4.1: every canonical prose change ships `Installed 2026-07-06, not yet proven in a live run`** — except the two field-attestation upgrades (Primitive 9 field closure; R3 → field-proven), which record already-observed production firings rather than new unproven rules. No code.
- 2026-06-29 — **Spec authored (DRAFT): Team-of-peers topology + live-prod audit — `docs/team-of-peers-and-prod-audit-spec.md`.** From a review of the 6 eXp Onboarding weekend transcripts (2026-06-27→29, reviewed via a 26-agent verification Workflow, adversarially verified — one cross-lens lost to the `[PROCESS-3]` retry cap, logged in-spec). The weekend field-ran a coordination shape the framework doesn't document: **three long-lived co-equal role windows** (Systems Architect / UI Designer / UX Engineer) reviewing each other through an append-only **"Parts ledger"** doc (numbered single-owner Parts + lettered **Q-routing** to a named peer + one `Status: LOCKED` gate + provenance footer), handed off asynchronously via **`ScheduleWakeup`** (the `/loop` self-pacing wakeup — *not* `cron`), and a **live-prod UI audit** driving the running app with Playwright through a **disposable login** (3 windows, 122/95/46 calls vs `retrailhead.com`). It worked (Program Engine V2 shipped to prod; the prod audit caught a population-wide P0) but its failure modes were dodged by luck: the shared doc had no lock (`File modified since read` ×3), the LOCKED state lived in two places that drifted, and one wakeup fired **60 s** after the peer it waited on finished. Spec proposes 7 file-targeted changes across 4 parallel tracks — **Topology 3** + Parts-ledger template + concurrency rules + the `ScheduleWakeup` async-handoff guidance in `docs/MULTI_AGENT_WORKFLOW.md`; a `/prod-audit` skill carrying the safety contract (disposable account, no outward-facing side effects, logged-out flows last, read-only on code, **creds-from-env-not-prompt**); a thin `/convene` entry point; `plan-review` Step-6a Q-routing + single-writer-header checks; and `docs/LESSONS.md` `[PROCESS-6]` **smoke the user's end-to-end job, not the component you built** + a fail-loud guard for the schema-forced-fan-out drop. **DRAFT — run `/plan-review` to LOCK before `/implement`.** No code yet; implementation continues on another machine. Side-finding: the framework's own field copy (eXp `MULTI_AGENT_WORKFLOW.md`, 542 lines) has drifted **ahead** of canonical (408) — the un-harvested-downstream gap. Product P0 (eXp app, not framework) handed off in the spec appendix: the booking step references a `Schedule` button that `BookingCTACard.tsx:78` suppresses when the sponsor has no `booking_url`.
- 2026-06-26 — **Reverse sister-framework port executed + `[PROCESS-5]` codified.** The mirror of the v0.3.0 app→agent `sister-framework-adoption-spec`: this framework's verification/safety layer (v0.4.0–v0.5.1) was ported **into app-blueprint**, field-proven against 12 real downstream transcripts (9 eXp Onboarding + 3 Kai-App), narrow-and-expanded by that evidence, and shipped **in the app-blueprint repo as v0.2.0** (commit `2d3ef23`, GitHub release live — *not* a change to this repo's files). The proving **inverted the draft's priorities**: the centerpiece independent-refuter (A1) had a mechanism that never fired downstream (smokes caught the RLS-to-prod bugs, not refuters) → demoted + re-led by live-SQL re-grounding; the safety-primitives KB (B1) had 0 field instances → reference-only; and the two loudest real gaps (deferred-smoke debt **N1**, service-boundary live-smoke gate **N2**) were absent from the draft entirely. This repo's only change is the lesson learned: `docs/LESSONS.md` gains **`[PROCESS-5]` — field-prove a cross-framework port against the receiving side's transcripts before locking; expect inversion** (the spec-scale, cross-framework instance of `[PROCESS-1]` Corollary 1). Side-finding: the sister-framework-adoption pattern is now **proven bidirectional**. Per the discipline it documents, the `[PROCESS-5]` prose is itself `Installed, not yet proven in a live run`. No code.
- 2026-06-26 — **v0.5.1 — post-v0.5.0 verification & safety hardening** (bundles the two 2026-06-26 batches below). Net of the release: `docs/LESSONS.md` `[PROCESS-1]` gains an **input-side corollary** (a prior analysis is relayed-until-reground) *and* a **runtime corollary** (shipped code is unproven until its trigger fires + you inspect the persisted artifact); plus `[PROCESS-3]` (a schema-forced fan-out can silently under-cover) and `[PROCESS-4]` (a fan-out's synthesis is the least-trustworthy layer). `OC_KB_11` gains **Primitive 9 — Fail loud or fail closed; never fail silent-open** (count 8→9). `OC_KB_13` gains an **outcome-vs-output** anti-pattern + a **concrete-defect-over-abstract-lesson** refinement; `/retro` gains an outcome check. All learned and adversarially filtered from the kai downstream proving runs (a phases-1-4 build session, a 75-agent phases-5-8 workflow, and four live HP/TMT catch-ups), and all `Installed, not yet proven in a live run`. No code.
- 2026-06-26 — **Second post-v0.5.0 batch: verification/safety refinements from the kai downstream catch-up audits, adversarially filtered.** A completeness critic — re-run as a *prose* agent after the schema-forced one hit the `[PROCESS-3]` retry cap (the lesson catching its own author) — surfaced a fourth gap beyond three sharpens. Shipped, all no-code: (1) `docs/LESSONS.md` `[PROCESS-1]` **Corollary 2** — the runtime twin: a shipped *code* path is unproven until its trigger fires *and* you've inspected the persisted artifact, not the digest ("deployed ≠ exercised" — 5/6 phase-5-8 deliverables were dormant, and a `PGRST204` regression hid trapped-as-a-Warning behind a green "9 synced" digest). (2) `OC_KB_11` **Primitive 9 — Fail loud or fail closed; never fail silent-open** (+ anti-pattern + diagnosis step; primitive count 8→9) — the defensive-write fail-mode primitive, the authoring-side complement of Corollary 2. (3) `OC_KB_13` **"output metrics without a paired outcome metric"** anti-pattern + a `/retro` outcome-vs-output check — measure surface reduction, not artifacts shipped (23 artifacts shipped, surface didn't move). (4) `OC_KB_13` **concrete-defect-over-abstract-lesson** refinement to the "classification is the value" claim. The filter deliberately DROPPED three candidates: a `/audit-run` skill (over-fits a maintainer-reviewing-a-peer activity; primitives already exist — `context-at-turn.mjs` + OC_KB_08 replay; collides with OC_KB_08's anti-premature-observability stance), consolidating verification into one KB (premise false — the PROCESS lessons are already one contiguous, by-ID-cited section), and a workflow-cost framing (redundant with `[PROCESS-4]`). Installed, not yet proven in a live run.
- 2026-06-26 — **Post-v0.5.0 verification-discipline refinements, learned from the kai-openclaw downstream proving runs** (phases 1–8 build sessions + two live HP/TMT catch-ups, each adversarially re-verified here). `docs/LESSONS.md` gains three lessons: a `[PROCESS-1]` **corollary** — a prior analysis / audit / roadmap you *execute against* is `relayed`, not `read-it`, until re-grounded (a 424-line kai audit was idealized or wrong in all four phases it drove, incl. a silent 883-row financial truncation it never named); `[PROCESS-3]` — a schema-forced Workflow fan-out can silently *under-cover* (an `agent({schema})` that exhausts the retry cap returns `null`; log the shortfall, prefer small/flat schemas); and `[PROCESS-4]` — a fan-out's synthesized verdicts/summary are the *least-trustworthy* layer, so re-verify the primary artifacts and never land on the summary's word. The kai runs **proved the principle** of each: a 75-agent / ~5.4M-token workflow's verdicts were wrong in both directions, and the re-verify discipline caught **six** would-be regressions (some psql-FATAL) before prod; in the live catch-ups the agent re-grounded an approved-but-wrong close and held it. Per the discipline itself — the incidents are observed and grounded, but the lesson *prose* is `Installed, not yet proven in a live run` (proven only when an agent that *lacks* the instinct consults the lesson and acts differently). No code.
- 2026-06-25 — **v0.5.0 — *Insecure Agents* (Sentry) two-talk framework review (T1+T2+T3 bundled; T1 `f7cdd23`, T2 `a506906`).** Tier 3 adds `docs/LESSONS.md` `[PROCESS-2]` "Augment the deterministic baseline; don't replace it" (the verification-side twin of `[SKILL-1]`), an `OC_KB_02` MCP-vs-CLI auth-boundary note, an `OC_KB_01` "build vs buy: anchor on the data, not the harness" note, an `OC_KB_05` confounded-model-swap anti-pattern, and the release's one piece of code: `_dev/tools/validate-skills.mjs`, a zero-dependency Node validator for the CLAUDE.md DO-NOT skill traps (folder==name, `user-invokable` spelling, bootstrap char-cap) homed in `_dev/tools/` (dev/CI), not the rsync-deployed `workspace/scripts/`. **Verification ledger: 2 verified, rest installed-not-proven.** VERIFIED (code): `validate-skills.mjs` catches a folder/name mismatch and the `user-invocable` typo on fixtures (exit 1), passes clean on the repo (exit 0), `node --check` passes. VERIFIED (behavior): the `/gen-test` "Before you trust these tests" discipline fired in an independent agent run (it labeled its generated tests an "unverified self-report") — proves it fires, not that it improves test quality over time. All other v0.5.0 additions (`[PROCESS-1]`, `[PROCESS-2]`, OC_KB_11 "Primitive 0", the `references/` accuracy-lever reframe, the MCP-boundary note, build-vs-buy, the confounded-model-swap anti-pattern, the two **(Multi-user only)** notes) are `Installed, not yet proven in a live run`. The adversarial filter deliberately dropped two candidates: an "over-decomposition" anti-pattern (single confounded anecdote) and an audit-infra check (false-positive generator for a single-tenant agent).
- 2026-06-25 — Tier 2 of the *Insecure Agents* (Sentry) talk review (insights grounded against real files, adversarially filtered by three skeptics): four framework-level prose additions, no code. `OC_KB_02_Skills.md` reframes the `references/` directory as also a proactive **accuracy lever** (synthesize prior art to raise the model's hit-rate, not only to evict context overflow), keeping the honest authoring cost. `.claude/commands/gen-test.md` gains a "Before you trust these tests" note (an agent-written test is an unverified self-report — mutation-check load-bearing assertions by hand, watch for tests that mirror the implementation or assert on a mock's own return, treat generated tests as additive signal layered on CI + smokes, not a replacement). `OC_KB_04_Bootstrap_Files.md` gains a "(Multi-user only) MEMORY.md is a single-operator store" note (don't pool multiple users' facts in one MEMORY.md; per-operator/per-session scoping is the safe default; shared cross-user memory is unsolved — default single-tenant agent unaffected). `OC_KB_11_Safety_Primitives.md` gains a "Least privilege: no keys in context, default-deny in shared contexts" section before the eight primitives (the model never holds credentials — keys live in the plist, read by MCP server processes; plus a multi-user default-deny query-scoping rule enforced at the MCP tool/script boundary, never in skill prose). The adversarial filter deliberately excluded INS-05's "over-decomposition" anti-pattern (single confounded anecdote; contradicts existing decomposition guidance) and INS-10's audit-infra deterministic check (false-positive generator for a single-tenant agent). Installed, not yet proven in a live run.
- 2026-06-25 — Tier 1 of the *Insecure Agents* (Sentry) talk review (insights grounded against real files, adversarially filtered by three skeptics): two framework-level prose additions, no code. `docs/LESSONS.md` gains a `## PROCESS` section with `[PROCESS-1]` "A prose change is unproven until a live run" — the verification-side twin of `[SKILL-1]`: read-back on disk proves text exists, not that behavior changed, so prose edits carry the `Installed, not yet proven in a live run` flag and externally-sourced/viral techniques stay unproven until they survive a run here. `OC_KB_11_Safety_Primitives.md` gains a `## Primitive 0 — Don't expose the operation (close the capability)` (before the existing eight) plus a one-line forward-reference in the Pattern intro: the strongest safety move is to not put a destructive op on the agent's surface at all (don't register `delete_*` tools in `mcporter.json`; route rare needs to a human UI), with the prompt-injection threat model and a single-user-vs-enterprise risk dial. Tier 2 deferred. Installed, not yet proven in a live run.
- 2026-06-25 — **v0.4.1 — Skill/script-boundary guidance: keep judgment in the skill, mechanics in a script.** Encodes a framework-level lesson as a prevent/detect/explain set across four files: `OC_KB_02_Skills.md` gains a "The mixed case: split judgment from mechanics" section (the dry-run handoff pattern) + an "deterministic logic embedded in skill prose" anti-pattern + a validation-checklist item; `_dev/skill-template.md` gains determinism guidance at the Workflows section (prevent at creation); `.claude/commands/audit-code.md` gains an over-determinism anti-pattern row (detect in review, guarded so query *templates* aren't flagged); `docs/LESSONS.md` gains `[SKILL-1]` as the decision record, and the ships-empty note now distinguishes framework-level lessons (which propagate) from app-specific ones. Originated from a read-only assessment of a production agent's six longest skills — all over-reliant on embedded deterministic logic (one re-described in prose the CSV parsing a 68 KB script already did). Authored in the canonical framework so it propagates downstream via `/update-framework`. An independent refutation pass caught one false-positive, so the guidance ships with a "don't over-extract / verify per-block" caveat. Tagged `v0.4.1`.
- 2026-06-25 — Build the verification agenda's open instrumentation item (kai findings §7): `_dev/tools/context-at-turn.mjs`, a deterministic Node CLI that parses a Claude Code `.jsonl` transcript and reconstructs, for any chosen turn, which files were actually in context (via `Read`/`Edit`/`Write`) plus a `claimedButNeverInContext` discrepancy list (sources the output cited authoritatively but never loaded — the "trusted a self-report" signal made testable). Unlike the three prose agenda threads, this tool was TESTED against real transcripts (the kai investigation transcript + this session): turn targeting, file reconstruction, and the claimed-vs-actual signal all validated. The OpenClaw gateway `transcript.json` reader is a deliberate unimplemented stub (schema unverifiable from a dev host — not guessed). Spec: `docs/context-instrumentation-spec.md` (LOCKED); discoverability pointer in `OC_KB_08`.
- 2026-06-25 — **v0.4.0 — Verification discipline agenda complete (threads 1–3).** Bundles thread 3 closure provenance (`c48f93d`), thread 2 falsification primitive (`7aea047`), and thread 1 ground-first anchor (below). Tagged `v0.4.0`.
- 2026-06-25 — Install the ground-first primary-artifact anchor (thread 1, final thread, of the verification agenda): `/investigate` gains a "Step 0: Anchor on the Primary Artifact" — quote the literal artifact, derive the §2 entry point from it (not from a theory of what's wrong), and mark downstream root-cause claims UNVERIFIED when the artifact is unavailable — with checkable `Primary Artifact` + `Entry point derived from:` output fields. `/brainstorm` Phase 1 gains one conditional artifact-anchor line reusing the existing `[verified]`/`[relayed]` provenance vocabulary. The grounded brainstorm (`wf_a1014478-c50`) confirmed thread 1 was otherwise already covered (rules #13/227/124, `/debug` Step 1, `/plan-review` §3a); only the `/investigate:37` entry-point seam was genuinely uncovered. Spec: `docs/ground-first-anchor-spec.md` (LOCKED). Installed, not yet proven in a live run.
- 2026-06-25 — Install the falsification primitive (thread 2 of the verification agenda): the audit suite (`/audit-code`, `/audit-infra`, `/audit-full`) now ends in an independent, graded **Refutation Ledger** — a fresh skeptic agent tries to KILL each load-bearing finding against primary source — superseding the self-graded `APPROVED`/`PASSED` checkbox, bounded by a fixed CLAUDE.md DO-NOT allowlist the producing auditor cannot shrink, with a mechanical tally and an explicit false-negative no-op caveat. `/debug` binds a mandatory independent refutation at the three-strikes boundary; `/orchestrate` Phase 8 Stage 2 consumes the ledger. Spec: `docs/falsification-primitive-spec.md` (LOCKED). Installed, not yet proven in a live run.
- 2026-06-24 — Install closure "done-vs-true" truth-discipline (thread 3 of the verification agenda): `/ship` Step 3.5 now gates on not-`Passed` (a never-run smoke surfaces verbatim as `Unverified at ship: <id>`), and a verified-vs-relayed provenance block is inlined at 5 synthesis sites across `/ship`, `/orchestrate`, `/retro`, `/brainstorm`. Spec: `docs/done-vs-true-closure-spec.md` (LOCKED).
- 2026-06-24 — Add grounding-findings investigation doc capturing the kai verification-theory grounding pass (16-claim ledger, the synthesis-amplifies-hedges meta-finding) and the 3-thread framework "done-vs-true" agenda.
