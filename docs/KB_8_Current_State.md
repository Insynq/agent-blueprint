# KB 8 — Current State

> This is the active tracking file. Keep it current — every Claude session reads it to orient on where the project stands.

## Active Phase
[TODO — populate during `/kickoff` or `/adopt`. Format: `Phase N — <name> — <status>`]

## Session Notes
- Skill/script-boundary lesson now encoded framework-wide and released as **v0.4.1 (2026-06-25)**: keep judgment in the skill, mechanics in a script. Lives as a prevent/detect/explain set — `OC_KB_02` "The mixed case" + anti-pattern + checklist item, `_dev/skill-template.md` Workflows-section determinism note, `.claude/commands/audit-code.md` over-determinism row, and `docs/LESSONS.md` `[SKILL-1]` (the decision record). Originated from the kai skill-balance assessment (six longest skills all over-reliant on embedded deterministic logic); an independent refutation pass caught one false-positive, so the guidance carries a "don't over-extract / verify per-block" caveat. Authored in the canonical framework to propagate downstream via `/update-framework`.
- Framework-verification "done-vs-true" agenda (`docs/investigations/2026-06-24-kai-verification-grounding-findings.md` §6): **all three threads shipped and released as v0.4.0 (2026-06-25).** Thread 3 — closure "done-vs-true" discipline (spec `docs/done-vs-true-closure-spec.md`, LOCKED; `c48f93d`). Thread 2 — falsification primitive: independent Refutation Pass across the audit suite (`/audit-code`, `/audit-infra`, `/audit-full`) + a mandatory refutation bound at `/debug`'s three-strikes boundary; `/orchestrate` Phase 8 Stage 2 consumes the ledger (spec `docs/falsification-primitive-spec.md`, LOCKED; `7aea047`). Thread 1 — ground-first primary-artifact anchor: `/investigate` Step 0 (quote the literal artifact, derive the entry point from it, checkable `Primary Artifact` + `Entry point derived from:` fields) + a one-line `/brainstorm` Phase 1 anchor; the grounded brainstorm (`wf_a1014478-c50`) confirmed thread 1 was otherwise already covered (spec `docs/ground-first-anchor-spec.md`, LOCKED). All three are markdown-prose edits — installed and verified by read-back on disk, not yet exercised in a live `/investigate`, `/audit-*`, or 3-strike `/debug` run. **The open instrumentation item is now SHIPPED:** `_dev/tools/context-at-turn.mjs` reconstructs which files were actually in context at a chosen turn (via `Read`/`Edit`/`Write`) plus a `claimedButNeverInContext` discrepancy list — the "trusted a self-report" signal made testable. Unlike the three prose threads, this tool was TESTED against real transcripts (the kai investigation transcript + this session); turn targeting, file reconstruction, and the claimed-vs-actual signal all validated. Spec `docs/context-instrumentation-spec.md` (LOCKED); pointer in `OC_KB_08`. The agenda is otherwise complete. **Only remaining follow-up:** implement the OpenClaw `transcript.json` reader (`readOpenClawTranscript()`) against a real transcript on the runtime host — it is a deliberate stub today because the schema is unverifiable from a dev host (do not guess it).

## Changelog
<!-- Format: Phase X.Y — Description ✅ -->
[Empty — one-liner entries added as phases complete, typically by `/ship`.]
