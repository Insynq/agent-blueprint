# KB 8 — Current State

> This is the active tracking file. Keep it current — every Claude session reads it to orient on where the project stands.

## Active Phase
[TODO — populate during `/kickoff` or `/adopt`. Format: `Phase N — <name> — <status>`]

## Session Notes
- **Reverse sister-framework port complete (2026-06-26).** This framework's verification/safety layer was ported **into app-blueprint** and shipped there as **v0.2.0** (commit `2d3ef23`, GitHub release live; not a change to this repo's source — the artifacts live in the app-blueprint repo). Field-proven against 12 downstream transcripts (9 eXp Onboarding + 3 Kai-App), which inverted the draft's priorities (A1 refuter mechanism unproven → demoted + re-led by live-SQL re-grounding; B1 → reference-only at 0 field instances; **N1** deferred-smoke debt + **N2** service-boundary live-smoke gate surfaced as the loudest gaps, absent from the draft). This repo's only change: `docs/LESSONS.md` `[PROCESS-5]` (field-prove a cross-framework port against the receiving side's transcripts before locking — expect inversion); the sister-framework-adoption pattern is now **proven bidirectional** (app→agent v0.3.0, agent→app v0.2.0). **Pending downstream proof:** the app-blueprint A1/A2/N1/N2 disciplines carry `Installed, not yet proven in a live run` — first real proof is a dogfood `/audit-rls` re-grounding firing its Refutation Ledger; capture the first downstream firing as calibration. `[PROCESS-5]` itself is also `Installed, not yet proven in a live run`.
- The *Insecure Agents* (Sentry) two-talk review is now bundled and released as **v0.5.0 (2026-06-25)** — Tier 1 (`f7cdd23`), Tier 2 (`a506906`), and Tier 3 (this ship). Tier 3 adds `[PROCESS-2]` (augment the deterministic baseline), an `OC_KB_02` MCP-vs-CLI auth-boundary note, an `OC_KB_01` build-vs-buy note, an `OC_KB_05` confounded-model-swap anti-pattern, and the release's only code: `_dev/tools/validate-skills.mjs` (zero-dep validator for the CLAUDE.md DO-NOT skill traps). Ledger: **2 verified** (`validate-skills.mjs` exit-1-on-fixtures / exit-0-on-repo / `node --check`; `/gen-test` discipline fired in an independent run), **rest installed-not-proven in a live run.** The filter dropped INS-05 (over-decomposition) and INS-10 (audit-infra check).
- Tier 1 of the *Insecure Agents* (Sentry) talk review shipped (2026-06-25, incremental, no tag): two framework-level prose additions, no code. `docs/LESSONS.md` `[PROCESS-1]` "A prose change is unproven until a live run" (verification-side twin of `[SKILL-1]`) + `OC_KB_11` `Primitive 0 — Don't expose the operation (close the capability)`. Both are doc/KB prose — installed and verified by read-back on disk, **not yet proven in a live run.**
- Tier 2 of the same review shipped (2026-06-25, incremental, no tag): four framework-level prose additions, no code. `OC_KB_02` reframes `references/` as a proactive accuracy lever; `gen-test.md` adds a "Before you trust these tests" note (agent-written tests are unverified self-reports); `OC_KB_04` adds a "(Multi-user only)" MEMORY.md single-operator-store warning; `OC_KB_11` adds a "Least privilege: no keys in context, default-deny in shared contexts" section before the eight primitives. The filter deliberately dropped INS-05 (over-decomposition anti-pattern) and INS-10 (audit-infra deterministic check). Doc/KB prose only — verified by read-back on disk, **not yet proven in a live run.**
- Skill/script-boundary lesson now encoded framework-wide and released as **v0.4.1 (2026-06-25)**: keep judgment in the skill, mechanics in a script. Lives as a prevent/detect/explain set — `OC_KB_02` "The mixed case" + anti-pattern + checklist item, `_dev/skill-template.md` Workflows-section determinism note, `.claude/commands/audit-code.md` over-determinism row, and `docs/LESSONS.md` `[SKILL-1]` (the decision record). Originated from the kai skill-balance assessment (six longest skills all over-reliant on embedded deterministic logic); an independent refutation pass caught one false-positive, so the guidance carries a "don't over-extract / verify per-block" caveat. Authored in the canonical framework to propagate downstream via `/update-framework`.
- Framework-verification "done-vs-true" agenda (`docs/investigations/2026-06-24-kai-verification-grounding-findings.md` §6): **all three threads shipped and released as v0.4.0 (2026-06-25).** Thread 3 — closure "done-vs-true" discipline (spec `docs/done-vs-true-closure-spec.md`, LOCKED; `c48f93d`). Thread 2 — falsification primitive: independent Refutation Pass across the audit suite (`/audit-code`, `/audit-infra`, `/audit-full`) + a mandatory refutation bound at `/debug`'s three-strikes boundary; `/orchestrate` Phase 8 Stage 2 consumes the ledger (spec `docs/falsification-primitive-spec.md`, LOCKED; `7aea047`). Thread 1 — ground-first primary-artifact anchor: `/investigate` Step 0 (quote the literal artifact, derive the entry point from it, checkable `Primary Artifact` + `Entry point derived from:` fields) + a one-line `/brainstorm` Phase 1 anchor; the grounded brainstorm (`wf_a1014478-c50`) confirmed thread 1 was otherwise already covered (spec `docs/ground-first-anchor-spec.md`, LOCKED). All three are markdown-prose edits — installed and verified by read-back on disk, not yet exercised in a live `/investigate`, `/audit-*`, or 3-strike `/debug` run. **The open instrumentation item is now SHIPPED:** `_dev/tools/context-at-turn.mjs` reconstructs which files were actually in context at a chosen turn (via `Read`/`Edit`/`Write`) plus a `claimedButNeverInContext` discrepancy list — the "trusted a self-report" signal made testable. Unlike the three prose threads, this tool was TESTED against real transcripts (the kai investigation transcript + this session); turn targeting, file reconstruction, and the claimed-vs-actual signal all validated. Spec `docs/context-instrumentation-spec.md` (LOCKED); pointer in `OC_KB_08`. The agenda is otherwise complete. **Only remaining follow-up:** implement the OpenClaw `transcript.json` reader (`readOpenClawTranscript()`) against a real transcript on the runtime host — it is a deliberate stub today because the schema is unverifiable from a dev host (do not guess it).

## Changelog
<!-- Format: Phase X.Y — Description ✅ -->
[Empty — one-liner entries added as phases complete, typically by `/ship`.]
